import React from "react";
import ReactDOM from "react-dom";
const headings = React.createElement("h1", {}, "Hello world!");
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(headings);
